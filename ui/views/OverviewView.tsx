// PR16D view skeleton — OverviewView.
import type { JSX } from "react";

export function OverviewView(): JSX.Element {
  return (
    <div role="region" aria-label="OverviewView">
      <h2>Overview</h2>
      <p>{"Workbench view skeleton — data injected by relic.ui API in implementation milestones."}</p>
    </div>
  );
}
